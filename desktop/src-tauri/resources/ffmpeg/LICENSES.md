# FFmpeg notices

QM Unlock uses FFmpeg only when the user selects MP3 output. The bundled
FFmpeg 8.0.1 executable and statically linked LAME 3.100 encoder are under
LGPL terms. The exact license texts are included in `COPYING.LGPLv2.1` and
`COPYING.LAME`; source URLs, digests, and configure details are in `SOURCE.md`.
